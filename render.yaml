services:
  - type: web
    name: youtube-flask-demo
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn api:app"
    plan: free
    envVars:
      - key: YOUTUBE_API_KEY
        sync: false
